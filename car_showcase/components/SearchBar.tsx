"use client";
import React from "react";

const SearchBar= () =>{
    const handleSearch = () => {
        
    }
    return(
        <form className="searchbar" onSubmit={handleSearch}>

        </form>
    )
}

export default SearchBar
